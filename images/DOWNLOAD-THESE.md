# Add your images here (rename the files you uploaded)

The site expects these filenames in this `images/` folder. You already have all of them — they're the images you sent in chat. Rename each one exactly as shown (all lowercase) and place it here.

## Required

| Save as      | Which image |
|--------------|-------------|
| `logo.jpg`   | The round Ready Van Services emblem (van in a circle). |
| `hero.jpg`   | The photo of the white van parked in the driveway (side view). |
| `tools.jpg`  | The "Equipped. Prepared. Ready to work." graphic showing dollies, blankets, straps, tarps. |

## Before & After gallery (any order is fine)

Name the 9 branded before/after graphics `g1.jpg` through `g9.jpg`. It doesn't matter which goes to which number — just use all nine:

- office room cleared
- backyard trampoline removed
- garage cleanout (toys/clutter)
- storage room cleared
- living room couch removed
- kitchen area cleared (foam)
- front-door mattress/dresser removed
- garage boxes removed
- apartment boxes cleared

## Notes
- JPG or PNG both work; if a file is a PNG, you can still name it `.jpg` only if you actually keep it JPG — simplest is to match the real format. If yours are PNG, tell me and I'll switch the code to `.png`.
- Optional extras you sent (business card, horizontal sign, full-load photo) aren't required for the page, but we can add a "gallery" or "contact card" section later if you want.

Once these are in place, open `index.html` and everything shows.
